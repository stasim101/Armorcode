package com.personal.ingest.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class KafkaTopicConfiguration {
	/*
	 * @Bean public NewTopic createTopic(String topicName, int partitionCount, int
	 * replicaCount) { return
	 * TopicBuilder.name(topicName).partitions(partitionCount).replicas(replicaCount
	 * ).build(); }
	 */}
